name: ar-models
channels:
  - pytorch
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch>=2.1
      - transformers>=4.40,<5.0
      - tokenizers>=0.15
      - pandas
      - pyarrow
      - numpy
